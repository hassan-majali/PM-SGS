[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && npx prisma migrate deploy && npm start"
healthcheckPath = "/api/v1/clients"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
